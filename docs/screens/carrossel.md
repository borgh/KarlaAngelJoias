# Tela — Carrossel Instagram (`/carrossel`)

`admin/src/pages/Carousels.tsx`. Grid de cards, cada um com upload de imagem, legenda opcional, link opcional, toggle "Visível no site" e botão salvar individual (não é um formulário único — cada card salva por si). Botão "Novo item" no topo, sem limite de quantidade. Ver `docs/features/carrossel-instagram.md`.
